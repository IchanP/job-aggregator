import { LinkedinJob } from "../boards/linkedin";

export async function FilterById(jobs: Array<LinkedinJob>) {
  // This function should:
  // Filter jobids that have already been seen by looking through SQLite
}

//  TODO - Filter jobs with non-latin titles?
